{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Vercel Build and Deploy",
            "type": "shell",
            "command": "vercel build && vercel deploy --prebuilt",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}